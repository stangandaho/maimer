% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traprate.R
\name{mm_get_effort}
\alias{mm_get_effort}
\title{Calculate camera trap deployment effort}
\usage{
mm_get_effort(
  deployment_data,
  start_column,
  end_column,
  deployment_column,
  format = "\%Y-\%m-\%d \%H:\%M:\%OS",
  time_zone = "",
  time_unit = "days"
)
}
\arguments{
\item{deployment_data}{A data frame containing camera trap deployment records.}

\item{start_column}{The column name (unquoted or as a string) indicating deployment start datetime.}

\item{end_column}{The column name (unquoted or as a string) indicating deployment end datetime.}

\item{deployment_column}{The column name (unquoted or as a string) that uniquely identifies the deployment (e.g., camera ID).}

\item{format}{A character string specifying the format of the datetime columns. Default is \code{"\%Y-\%m-\%d \%H:\%M:\%OS"}.}

\item{time_zone}{The time zone used to parse the datetime columns. Default is \code{""} (i.e., system time zone).}

\item{time_unit}{The unit in which to compute the effort duration.
Can be \code{"secs"}, \code{"mins"}, \code{"hours"}, \code{"days"}, or \code{"weeks"}. Default is \code{"days"}.}
}
\value{
A data frame with columns:
\itemize{
\item \code{deployment_column}: Deployment identifier
\item \code{effort}: Numeric value of monitoring effort
\item \code{effort_unit}: The time unit used
}
}
\description{
Computes the monitoring effort (e.g., in days) for each camera deployment
based on start and end timestamps.
}
\examples{
data("camtrapdp")
deployments <- camtrapdp$data$deployments
mm_get_effort(deployment_data = deployments,
              deployment_column = deploymentID,
              start_column = start,
              end_column = end)

}
\seealso{
\code{\link[=mm_traprate_data]{mm_traprate_data()}}
}
