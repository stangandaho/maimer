% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mm_plot_rose_diagram.R
\name{mm_plot_rose_diagram}
\alias{mm_plot_rose_diagram}
\title{Plot a 24-hour rose diagram of daily activity}
\usage{
mm_plot_rose_diagram(
  data = NULL,
  times,
  frequencies = "absolute",
  hide_lebels = FALSE,
  label_position = NULL,
  label_style = list(),
  time_range = 1,
  ci_segment = TRUE,
  mean_segment = TRUE,
  ring = TRUE,
  color = "gray20",
  fill = color,
  ci_style = list(),
  mean_style = list(),
  start = -0.12,
  width = NULL
)
}
\arguments{
\item{data}{A data frame containing the time values. If \code{NULL}, \code{times} must be provided as a vector.}

\item{times}{A numeric vector of time values (in radians) or a column name from \code{data}.}

\item{frequencies}{Character. Use \code{"absolute"} to show counts or \code{"relative"} to show percentages. Default is \code{"absolute"}.}

\item{hide_lebels}{Logical. If \code{TRUE}, frequency value labels on top of bars are hidden. Default is \code{FALSE}.}

\item{label_position}{Numeric. Controls vertical position of the frequency value labels (if shown).}

\item{label_style}{A list of styles for labels. Accepts \code{color}, \code{size}, and \code{family}.}

\item{time_range}{Numeric. Width of the time bins in hours. Default is \code{1} (hourly bins).}

\item{ci_segment}{Logical or numeric. If \code{TRUE}, a segment representing the 95\% confidence interval is added.
If numeric, this value sets the length of the CI ticks. Default is \code{TRUE}.}

\item{mean_segment}{Logical. If \code{TRUE}, a segment representing the mean time is added. Default is \code{TRUE}.}

\item{ring}{Logical or numeric vector. If \code{TRUE}, a default ring range is set.
If a numeric vector of length 2 is provided, sets custom inner and outer limits of the radial axis.}

\item{color}{Color of the bar border and segments. Default is \code{"gray20"}.}

\item{fill}{Fill color of the bars. Default is the same as \code{color}.}

\item{ci_style}{A list of styles for the confidence interval segment. Accepts \code{color}, \code{linetype}, and \code{linewidth}.}

\item{mean_style}{A list of styles for the mean segment. Accepts \code{color}, \code{linetype}, and \code{linewidth}.}

\item{start}{Numeric. The angle (in radians) where the polar plot starts. Default is \code{-0.12}.}

\item{width}{Numeric. Width of each bar. Default is \code{NULL}, which uses the default width from \code{geom_col()}.}
}
\value{
A \code{ggplot} object representing the rose diagram.
}
\description{
This function generates a rose diagram (circular bar plot) to visualize
daily activity patterns over a 24-hour period. Each bar represents either
the absolute or relative frequency of observations within hourly intervals.
The plot also includes a segment indicating the mean activity time, and
an optional segment showing the 95\% confidence interval of the activity period.
}
\examples{
set.seed(129)
library(dplyr)
library(ggplot2)

rf <- runif(123, 0, max = 6)

mm_plot_rose_diagram(data = NULL,
                     times = rf,
                     frequencies = "relative",
                     label_style = list(size = 4, color = 'red'),
                     label_position = 11,
                     time_range = 1,
                     mean_segment = TRUE,
                     ci_segment = 1,
                     ring = c(-5, 12),
                     color = 'gray20',
                     mean_style = list(linetype = 1, linewidth = .5, color = 'red'),
                     ci_style = list(linetype = 1, linewidth = .5, color = 'black')
)
}
